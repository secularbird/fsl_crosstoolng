#!/bin/sh
# WARNING: This script is only an example, the result of running this depends 
# on host machine and the environment variable you have set.
#

# Do nothing here, gcc are patched and configured by crosstool-ng
# the patches located at ./patch directory, which within FreeScale release package

# You can add some codes here to check if the gcc is already processed correctly